Readme for jQuery-clock-following-mouse-cursor

Изначально часы показывают текущее время и дату.
При выборе новых значений они записываются в куки и на часах устанавливается новое время и дата.
При входе на сайт сначала проверяется куки, если они существуют, то берутся значения даты и времени
и устанавливаются как текущие.

Stasonix compilation.